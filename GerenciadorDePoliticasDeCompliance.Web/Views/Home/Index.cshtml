@model GerenciadorDePoliticasDeCompliance.Web.Models.Politicas.ListaViewModel;

@{
    ViewData["Title"] = "Políticas de Compiance da Wiz";
}
<section>
    <section class="container-fluid col-xl-9 justify-content-center h-100 ">
        <section class="form-container shadow-lg" class="col-12 col-sm-6 col-md-3 mb-4" style="border-radius:6px">
            <h1 class="text-center mb-4">@ViewData["Title"]</h1>
            <div>
                <div>
                    <table class="table table-striped table-hover">
                        <thead>
                            @if (Model.Politicas.Count() == 0)
                            {
                                <div class="alert alert-danger text-center role="alert">
                                    Não há políticas!
                                </div>
                            }
                            else
                            {
                                <tr class="bg-info col-lg-12">
                                    <th class="text-center">
                                        Título

                                    </th>

                                    <th class="text-center">
                                        Data
                                    </th>

                                </tr>
                                @foreach (var item in Model.Politicas)
                                {
                                    <tr>

                                        <td>
                                            <a href="/Home/Detalhes/@item.Id">
                                                @Html.DisplayFor(modelitem => item.Titulo)
                                            </a>
                                        </td>
                                        <td class="text-center">
                                            @Html.DisplayFor(modelitem => item.Data)
                                        </td>
                                    </tr>
                                }
                            }
                        </thead>
                    </table>
                </div>
            </div>
            <p class="text-center">
                <a href="/Home/Assinados" class="btn btn-primary mb-4">Políticas Assinadas</a>
            </p>
        </section>
    </section>
</section>  